services:
  backend:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    # for api keys
    env_file:
      - ./.env

  web:
    build: ./web
    ports:
      - "3000:3000"
